import 'package:flutter/material.dart';
import 'base_chat_screen.dart';

class FacultyChatScreen extends StatelessWidget {
  const FacultyChatScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const BaseChatScreen(
      chatTitle: "Faculty Mentor",
    );
  }
}
